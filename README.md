## You need to install both PyQtWebEngine & PyQt5 modules for this. Copy the following commands and paste it inside your terminal to install the required packages.

### pip install PyQtWebEngine

### pip install PyQt5
